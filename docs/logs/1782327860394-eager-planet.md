# E4 — Offline-sync: реальний план виконання

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Виправити offline-sync: payload format, background sync, retry, drive bridge, tombstone guard, тести.

**Architecture:** Flutter Drift DB → SyncClient → security-api → Frappe/MariaDB

**Tech Stack:** Flutter/Dart (Drift, http, connectivity_plus), Python/FastAPI, unittest

## Глобальні обмеження
- Кожен файл писати окремо, перевіряти компіляцію перед наступним
- TDD: тест першим → побачити що падає → код що зеленить
- Не додавати коментарі якщо не просять
- Робоча тека: `/home/joker/RIAD CRM`

---

## Task 1: Виправити payload format у visit_detail_screen.dart

**Files:**
- Modify: `riad_mobile/lib/ui/visit/visit_detail_screen.dart:29-38`
- Test: ручна перевірка компіляції

**Interfaces:**
- Consumes: `SyncClient.pushPending()` очікує `{"scalars": {...}, "additive": {...}}`
- Produces: PendingOp з правильним форматом

- [ ] **Step 1: Прочитати файл**

```
riad_mobile/lib/ui/visit/visit_detail_screen.dart
```

- [ ] **Step 2: Змінити імпорти (рядок 1)**

Додати `import 'dart:convert';` після `import 'package:flutter/material.dart';`

- [ ] **Step 3: Змінити метод _changeStatus (рядки 29-38)**

БУЛО:
```dart
Future<void> _changeStatus(String newStatus) async {
    await widget.db.updateVisitStatus(widget.visitUuid, newStatus);
    await widget.db.createPendingOp(PendingOpsCompanion.insert(
      doctype: 'Visit',
      name: widget.visitUuid,
      op: 'update',
      payload: '{"status":"$newStatus"}',
      createdAt: DateTime.now().toUtc().millisecondsSinceEpoch,
    ));
  }
```

СТАЛО:
```dart
Future<void> _changeStatus(String newStatus) async {
    await widget.db.updateVisitStatus(widget.visitUuid, newStatus);
    await widget.db.createPendingOp(PendingOpsCompanion.insert(
      doctype: 'Visit',
      name: widget.visitUuid,
      op: 'update',
      payload: jsonEncode({
        'scalars': {'status': newStatus},
        'additive': {},
      }),
      createdAt: DateTime.now().toUtc().millisecondsSinceEpoch,
    ));
  }
```

- [ ] **Step 4: Перевірити компіляцію**

```bash
cd "/home/joker/RIAD CRM/riad_mobile" && flutter analyze lib/ui/visit/visit_detail_screen.dart
```

- [ ] **Step 5: Записати лог**

Створити файл `.mimocode/plans/e4-session1-log.md` з записом:
```
# E4 Session 1 Log
**Дата:** [поточна дата]
**Статус:** success
**Завдання:**
- [x] visit_detail_screen.dart — payload format виправлено
```

---

## Task 2: Виправити payload format у camera_screen.dart

**Files:**
- Modify: `riad_mobile/lib/ui/media/camera_screen.dart:84-90`

- [ ] **Step 1: Прочитати файл**

- [ ] **Step 2: Змінити імпорти**

Додати `import 'dart:convert';` після `import 'dart:io';`

- [ ] **Step 3: Змінити createPendingOp (рядки 84-90)**

БУЛО:
```dart
await widget.db.createPendingOp(PendingOpsCompanion.insert(
      doctype: 'MediaAsset',
      name: clientUuid,
      op: 'create',
      payload: '{"media_type":"photo","tag":"$_selectedTag"}',
      createdAt: DateTime.now().toUtc().millisecondsSinceEpoch,
    ));
```

СТАЛО:
```dart
await widget.db.createPendingOp(PendingOpsCompanion.insert(
      doctype: 'Media Asset',
      name: clientUuid,
      op: 'create',
      payload: jsonEncode({
        'scalars': {
          'media_type': 'photo',
          'tag': _selectedTag,
          'parent_doctype': widget.parentDoctype ?? '',
          'parent_name': widget.parentName ?? '',
        },
        'additive': {},
      }),
      createdAt: DateTime.now().toUtc().millisecondsSinceEpoch,
    ));
```

- [ ] **Step 4: Перевірити компіляцію**

```bash
cd "/home/joker/RIAD CRM/riad_mobile" && flutter analyze lib/ui/media/camera_screen.dart
```

- [ ] **Step 5: Оновити лог**

Додати до `e4-session1-log.md`:
```
- [x] camera_screen.dart — payload format + doctype 'Media Asset' виправлено
```

---

## Task 3: Виправити payload format у voice_note_screen.dart

**Files:**
- Modify: `riad_mobile/lib/ui/voice/voice_note_screen.dart:85-91`

- [ ] **Step 1: Прочитати файл**

- [ ] **Step 2: Змінити імпорти**

Додати `import 'dart:convert';` після `import 'dart:io';`

- [ ] **Step 3: Змінити createPendingOp (рядки 85-91)**

БУЛО:
```dart
await widget.db.createPendingOp(PendingOpsCompanion.insert(
      doctype: 'MediaAsset',
      name: clientUuid,
      op: 'create',
      payload: '{"media_type":"audio"}',
      createdAt: DateTime.now().toUtc().millisecondsSinceEpoch,
    ));
```

СТАЛО:
```dart
await widget.db.createPendingOp(PendingOpsCompanion.insert(
      doctype: 'Media Asset',
      name: clientUuid,
      op: 'create',
      payload: jsonEncode({
        'scalars': {
          'media_type': 'audio',
          'parent_doctype': widget.parentDoctype ?? '',
          'parent_name': widget.parentName ?? '',
        },
        'additive': {},
      }),
      createdAt: DateTime.now().toUtc().millisecondsSinceEpoch,
    ));
```

- [ ] **Step 4: Перевірити компіляцію**

```bash
cd "/home/joker/RIAD CRM/riad_mobile" && flutter analyze lib/ui/voice/voice_note_screen.dart
```

- [ ] **Step 5: Оновити лог**

```
- [x] voice_note_screen.dart — payload format + doctype 'Media Asset' виправлено
```

---

## Task 4: Виправити payload format у checklist_screen.dart

**Files:**
- Modify: `riad_mobile/lib/ui/checklist/checklist_screen.dart:32-53`

- [ ] **Step 1: Прочитати файл**

- [ ] **Step 2: Змінити імпорти**

Додати `import 'dart:convert';` після `import 'package:flutter/material.dart';`

- [ ] **Step 3: Замінити _toggleItem метод (рядки 32-53)**

БУЛО (два createPendingOp):
```dart
Future<void> _toggleItem(ChecklistInstanceItem item, bool checked) async {
    await (db.update(db.checklistInstanceItems)
      ..where((t) => t.itemUuid.equals(item.itemUuid)))
        .write(ChecklistInstanceItemsCompanion(
          checkedBy: Value(checked ? 'current_user' : null),
        ));

    await db.createPendingOp(PendingOpsCompanion.insert(
      doctype: 'ChecklistInstanceItem',
      name: item.itemUuid,
      op: 'update',
      payload: '{"checked_by":"${checked ? 'current_user' : ''}"}',
      createdAt: DateTime.now().toUtc().millisecondsSinceEpoch,
    ));

    await db.createPendingOp(PendingOpsCompanion.insert(
      doctype: 'ChecklistInstance',
      name: item.instanceUuid,
      op: 'update',
      payload: '{}',
      createdAt: DateTime.now().toUtc().millisecondsSinceEpoch,
    ));
  }
```

СТАЛО (один createPendingOp):
```dart
Future<void> _toggleItem(ChecklistInstanceItem item, bool checked) async {
    await (db.update(db.checklistInstanceItems)
      ..where((t) => t.itemUuid.equals(item.itemUuid)))
        .write(ChecklistInstanceItemsCompanion(
          checkedBy: Value(checked ? 'current_user' : null),
        ));

    await db.createPendingOp(PendingOpsCompanion.insert(
      doctype: 'Checklist Instance',
      name: item.instanceUuid,
      op: 'update',
      payload: jsonEncode({
        'scalars': {'status': 'in_progress'},
        'additive': {
          'checklist_instance_item': [
            {
              '_uuid': item.itemUuid,
              'checked_by': checked ? 'current_user' : null,
            },
          ],
        },
      }),
      createdAt: DateTime.now().toUtc().millisecondsSinceEpoch,
    ));
  }
```

- [ ] **Step 4: Перевірити компіляцію**

```bash
cd "/home/joker/RIAD CRM/riad_mobile" && flutter analyze lib/ui/checklist/checklist_screen.dart
```

- [ ] **Step 5: Оновити лог**

```
- [x] checklist_screen.dart — payload format + один PendingOp замість двох
```

---

## Task 5: Виправити payload format у scan_screen.dart

**Files:**
- Modify: `riad_mobile/lib/ui/scan/scan_screen.dart:42-55`

- [ ] **Step 1: Прочитати файл**

- [ ] **Step 2: Змінити імпорти**

Додати `import 'dart:convert';` після `import 'package:flutter/material.dart';`

- [ ] **Step 3: Змінити рядки 49-55**

БУЛО:
```dart
await widget.db.createPendingOp(PendingOpsCompanion.insert(
            doctype: 'VisitMaterial',
            name: clientUuid,
            op: 'create',
            payload: '{"serial_no":"$code","visit_uuid":"${widget.visitUuid}"}',
            createdAt: DateTime.now().toUtc().millisecondsSinceEpoch,
          ));
```

СТАЛО:
```dart
await widget.db.createPendingOp(PendingOpsCompanion.insert(
            doctype: 'Visit',
            name: widget.visitUuid,
            op: 'update',
            payload: jsonEncode({
              'scalars': {},
              'additive': {
                'visit_material': [
                  {
                    '_uuid': clientUuid,
                    'serial_no': code,
                    'item_name': code,
                    'qty': 1,
                  },
                ],
              },
            }),
            createdAt: DateTime.now().toUtc().millisecondsSinceEpoch,
          ));
```

- [ ] **Step 4: Перевірити компіляцію**

```bash
cd "/home/joker/RIAD CRM/riad_mobile" && flutter analyze lib/ui/scan/scan_screen.dart
```

- [ ] **Step 5: Оновити лог**

```
- [x] scan_screen.dart — payload format + doctype 'Visit' з additive visit_material
```

---

## Task 6: Запустити flutter analyze на всі змінені файли

- [ ] **Step 1: Запустити аналіз**

```bash
cd "/home/joker/RIAD CRM/riad_mobile" && flutter analyze lib/ui/visit/visit_detail_screen.dart lib/ui/media/camera_screen.dart lib/ui/voice/voice_note_screen.dart lib/ui/checklist/checklist_screen.dart lib/ui/scan/scan_screen.dart
```

Очікуваний результат: `No issues found!`

- [ ] **Step 2: Якщо є помилки — виправити**

- [ ] **Step 3: Оновити лог**

```
**Інтеграція:** flutter analyze — success
```

---

## Task 7: Запустити існуючі тести S1

- [ ] **Step 1: Запустити тести**

```bash
cd "/home/joker/RIAD CRM" && python -m unittest tests.s1.test_s1_sync -v
```

Очікуваний результат: всі 8 тестів PASS

- [ ] **Step 2: Якщо є помилки — виправити**

- [ ] **Step 3: Оновити лог**

```
**Тести S1:** [кількість PASS/total]
```

---

## Task 8: Запустити тести S2 (Flutter)

- [ ] **Step 1: Запустити тести**

```bash
cd "/home/joker/RIAD CRM/riad_mobile" && flutter test test/s2/sync_client_test.dart
```

Очікуваний результат: всі тести PASS

- [ ] **Step 2: Якщо є помилки — виправити**

- [ ] **Step 3: Оновити лог**

```
**Тести S2:** [кількість PASS/total]
```

---

## Task 9: Створити BackgroundSyncService

**Files:**
- Create: `riad_mobile/lib/data/sync/background_sync_service.dart`

- [ ] **Step 1: Створити файл**

```dart
import 'dart:async';
import 'package:connectivity_plus/connectivity_plus.dart';
import 'sync_client.dart';
import 'media_upload_service.dart';

class BackgroundSyncService {
  final SyncClient _syncClient;
  final MediaUploadService _mediaUploadService;
  Timer? _timer;
  bool _syncing = false;

  BackgroundSyncService({
    required SyncClient syncClient,
    required MediaUploadService mediaUploadService,
  })  : _syncClient = syncClient,
        _mediaUploadService = mediaUploadService;

  void start({Duration interval = const Duration(minutes: 5)}) {
    _timer?.cancel();
    _timer = Timer.periodic(interval, (_) => syncNow());
    Connectivity().onConnectivityChanged.listen((results) {
      final isOnline = results.any((r) => r != ConnectivityResult.none);
      if (isOnline) syncNow();
    });
    syncNow();
  }

  void stop() {
    _timer?.cancel();
    _timer = null;
  }

  Future<void> syncNow() async {
    if (_syncing) return;
    _syncing = true;
    try {
      await _syncClient.retryFailed();
      await _syncClient.pushPending();
      await _syncClient.pullDelta();
      await _mediaUploadService.uploadPending();
    } catch (_) {
    } finally {
      _syncing = false;
    }
  }
}
```

- [ ] **Step 2: Перевірити компіляцію**

```bash
cd "/home/joker/RIAD CRM/riad_mobile" && flutter analyze lib/data/sync/background_sync_service.dart
```

- [ ] **Step 3: Оновити лог**

```
- [x] background_sync_service.dart — створено
```

---

## Task 10: Додати retryFailed в sync_client.dart

**Files:**
- Modify: `riad_mobile/lib/data/sync/sync_client.dart` (додати метод)

- [ ] **Step 1: Прочитати файл**

- [ ] **Step 2: Додати метод retryFailed після метода watchPendingCount (рядок 461)**

```dart
Future<void> retryFailed() async {
    final failedOps = await (_db.select(_db.pendingOps)
      ..where((t) => t.status.equals('failed')))
        .get();
    for (final op in failedOps) {
      final createdAt = DateTime.fromMillisecondsSinceEpoch(op.createdAt);
      final elapsed = DateTime.now().difference(createdAt);
      if (elapsed.inMinutes < 5) continue;
      await _db.updatePendingOpStatus(op.id, 'pending');
    }
  }
```

- [ ] **Step 3: Перевірити компіляцію**

```bash
cd "/home/joker/RIAD CRM/riad_mobile" && flutter analyze lib/data/sync/sync_client.dart
```

- [ ] **Step 4: Оновити лог**

```
- [x] sync_client.dart — retryFailed метод додано
```

---

## Task 11: Виправити тест — private method

**Files:**
- Modify: `riad_mobile/test/s2/sync_client_test.dart:589`

- [ ] **Step 1: Прочитати файл (рядок 589)**

- [ ] **Step 2: Замінити**

БУЛО:
```dart
await syncClient._handleConflict('visit-server-resolve', [
```

СТАЛО:
```dart
await syncClient.handleConflict('visit-server-resolve', [
```

- [ ] **Step 3: Запустити тест**

```bash
cd "/home/joker/RIAD CRM/riad_mobile" && flutter test test/s2/sync_client_test.dart
```

- [ ] **Step 4: Оновити лог**

```
- [x] sync_client_test.dart — _handleConflict → handleConflict виправлено
```

---

## Task 12: Drive upload → sync bridge

**Files:**
- Modify: `riad_mobile/lib/data/sync/media_upload_service.dart:54`

- [ ] **Step 1: Прочитати файл**

- [ ] **Step 2: Додати import 'dart:convert';**

- [ ] **Step 3: Додати після рядка 54**

```dart
if (driveFileId != null) {
              await db.createPendingOp(PendingOpsCompanion.insert(
                doctype: 'Media Asset',
                name: upload.clientUuid,
                op: 'update',
                payload: jsonEncode({
                  'scalars': {'drive_file_id': driveFileId},
                  'additive': {},
                }),
                createdAt: DateTime.now().toUtc().millisecondsSinceEpoch,
              ));
            }
```

- [ ] **Step 4: Перевірити компіляцію**

```bash
cd "/home/joker/RIAD CRM/riad_mobile" && flutter analyze lib/data/sync/media_upload_service.dart
```

- [ ] **Step 5: Оновити лог**

```
- [x] media_upload_service.dart — drive upload → sync bridge додано
```

---

## Task 13: Tombstone resurrection guard

**Files:**
- Modify: `riad_mobile/lib/data/local/database.dart` (додати метод)

- [ ] **Step 1: Прочитати файл**

- [ ] **Step 2: Додати метод isTombstoned після метода watchChecklistItems (рядок 412)**

```dart
Future<bool> isTombstoned(String clientUuid) async {
    final visit = await (select(visits)
          ..where((t) => t.clientUuid.equals(clientUuid)))
        .getSingleOrNull();
    if (visit != null) return visit.riadDeleted;
    final instance = await (select(checklistInstances)
          ..where((t) => t.clientUuid.equals(clientUuid)))
        .getSingleOrNull();
    if (instance != null) return instance.riadDeleted;
    final map = await (select(installationMaps)
          ..where((t) => t.clientUuid.equals(clientUuid)))
        .getSingleOrNull();
    if (map != null) return map.riadDeleted;
    return false;
  }
```

- [ ] **Step 3: Перевірити компіляцію**

```bash
cd "/home/joker/RIAD CRM/riad_mobile" && flutter analyze lib/data/local/database.dart
```

- [ ] **Step 4: Оновити лог**

```
- [x] database.dart — isTombstoned guard додано
```

---

## Task 14: Інтеграційний тест Python

**Files:**
- Create: `tests/s1/test_s1_sync_integration.py`

- [ ] **Step 1: Створити файл**

```python
"""E4 Integration Test — push → conflict → resolve → pull"""
import asyncio
import json
import os
import sys
import unittest
from unittest.mock import AsyncMock, patch

_services_root = os.path.join(
    os.path.dirname(__file__), "..", "..", "services", "security-api"
)
if os.path.isdir(_services_root):
    sys.path.insert(0, _services_root)


def _run(coro):
    return asyncio.get_event_loop().run_until_complete(coro)


class TestSyncIntegration(unittest.TestCase):
    def test_push_conflict_resolve_pull_cycle(self):
        from app.schemas.sync import (
            SyncPullRequest,
            SyncPushItem,
            SyncPushRequest,
            SyncResolveRequest,
        )
        from app.services.sync_service import (
            _encode_watermark,
            pull_changes,
            push_batch,
            resolve_conflict,
        )

        existing_doc = {
            "data": {
                "name": "uuid-visit-1",
                "riad_version": 3,
                "riad_deleted": 0,
                "status": "Server",
                "engineer": "eng@riad.fun",
                "materials": [],
                "photos": [],
            }
        }
        conflict_doc = {
            "data": {
                "name": "SC-001",
                "conflict_doctype": "Visit",
                "conflict_docname": "uuid-visit-1",
                "conflict_field": "status",
                "server_value": "Server",
                "client_value": "Client",
                "resolved": 0,
            }
        }
        updated_visit = {
            "data": {
                "name": "uuid-visit-1",
                "riad_version": 4,
                "riad_deleted": 0,
                "status": "Client",
                "engineer": "eng@riad.fun",
                "materials": [],
                "photos": [],
            }
        }

        call_count = [0]

        def _get(path, params=None, sid=""):
            call_count[0] += 1
            if "Sync Conflict" in path:
                return conflict_doc
            if "Visit/uuid-visit-1" in path and call_count[0] > 5:
                return updated_visit
            return existing_doc

        with patch(
            "app.services.sync_service.frappe_get", side_effect=_get
        ), patch(
            "app.services.sync_service.frappe_post", new_callable=AsyncMock
        ) as mock_post, patch(
            "app.services.sync_service.frappe_put", new_callable=AsyncMock
        ) as mock_put:
            mock_post.return_value = {"data": {"name": "SC-001"}}
            mock_put.return_value = {"data": {}}

            push_req = SyncPushRequest(
                device_id="d1",
                batch=[
                    SyncPushItem(
                        doctype="Visit",
                        name="uuid-visit-1",
                        op="upsert",
                        client_base_version=2,
                        scalars={"status": "Client"},
                    )
                ],
            )
            push_result = _run(
                push_batch(push_req, user_id="eng", sid="sid")
            )
            self.assertEqual(push_result.results[0].status, "conflict")

            resolve_req = SyncResolveRequest(
                conflict_id="SC-001", chosen="client"
            )
            resolve_result = _run(
                resolve_conflict(resolve_req, user_id="eng", sid="sid")
            )
            self.assertEqual(resolve_result.status, "resolved")

            pull_req = SyncPullRequest(
                device_id="d1",
                watermark=_encode_watermark(
                    "2020-01-01 00:00:00.000000"
                ),
            )
            pull_result = _run(pull_changes(pull_req, sid="sid"))
            self.assertTrue(pull_result.next_watermark)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Запустити тест**

```bash
cd "/home/joker/RIAD CRM" && python -m unittest tests.s1.test_s1_sync_integration -v
```

Очікуваний результат: OK

- [ ] **Step 3: Оновити лог**

```
- [x] test_s1_sync_integration.py — створено та пройдено
```

---

## Task 15: Фінальний запуск всіх тестів

- [ ] **Step 1: S1 тести**

```bash
cd "/home/joker/RIAD CRM" && python -m unittest tests.s1.test_s1_sync -v
```

- [ ] **Step 2: S1 інтеграційний тест**

```bash
cd "/home/joker/RIAD CRM" && python -m unittest tests.s1.test_s1_sync_integration -v
```

- [ ] **Step 3: S2 тести (Flutter)**

```bash
cd "/home/joker/RIAD CRM/riad_mobile" && flutter test test/s2/sync_client_test.dart
```

- [ ] **Step 4: Flutter analyze**

```bash
cd "/home/joker/RIAD CRM/riad_mobile" && flutter analyze
```

- [ ] **Step 5: Фінальний лог**

Оновити `.mimocode/plans/e4-session1-log.md`:

```
**Фінальний статус:** success
**Всі тести:** PASS
**Всі файли:** змінено/створено
```

---

## Структура логу

Після виконання кожної Task записувати у `.mimocode/plans/e4-session1-log.md`:

```markdown
# E4 Session 1 Log

**Дата:** YYYY-MM-DD HH:MM
**Статус:** success | partial | failed

## Завдання:
- [x] Task 1 — visit_detail_screen.dart payload
- [x] Task 2 — camera_screen.dart payload
- ...

## Тести:
- S1: [PASS/total]
- S2: [PASS/total]
- Flutter analyze: OK/errors

## Інтеграція:
- Чи пройшли тести: yes/no
- Чи є зміни в інших файлах: yes/no

## Наступна сесія:
- Task 9-13 (background sync, retry, bridge, guard)
```

---

# ПРОМТИ ДЛЯ КОЖНОЇ СЕСІЇ

---

## ПРОМТ СЕСІЇ 1: Виправлення payload format (Task 1-6)

```
Ти — Flutter розробник. Виконай завдання.

## Контекст
Є offline-sync система. `sync_client.dart` метод `pushPending()` (рядки 321-330) очікує що payload PendingOp має формат:
{"scalars": {"field": "value"}, "additive": {"child_table": [{"_uuid": "...", ...}]}}

Але ВСІ screens створюють PendingOps з плоским payload. Це критичний баг — жоден sync не працює.

## Завдання
Виправити 5 файлів:

1. `riad_mobile/lib/ui/visit/visit_detail_screen.dart` — метод `_changeStatus` (рядки 29-38):
   - Додати `import 'dart:convert';`
   - Замінити `payload: '{"status":"$newStatus"}'` на `payload: jsonEncode({'scalars': {'status': newStatus}, 'additive': {}})`

2. `riad_mobile/lib/ui/media/camera_screen.dart` — метод `_takePicture` (рядки 84-90):
   - Додати `import 'dart:convert';`
   - Замінити `doctype: 'MediaAsset'` на `doctype: 'Media Asset'`
   - Замінити payload на `jsonEncode({'scalars': {'media_type': 'photo', 'tag': _selectedTag, 'parent_doctype': widget.parentDoctype ?? '', 'parent_name': widget.parentName ?? ''}, 'additive': {}})`

3. `riad_mobile/lib/ui/voice/voice_note_screen.dart` — метод `_saveRecording` (рядки 85-91):
   - Додати `import 'dart:convert';`
   - Замінити `doctype: 'MediaAsset'` на `doctype: 'Media Asset'`
   - Замінити payload на `jsonEncode({'scalars': {'media_type': 'audio', 'parent_doctype': widget.parentDoctype ?? '', 'parent_name': widget.parentName ?? ''}, 'additive': {}})`

4. `riad_mobile/lib/ui/checklist/checklist_screen.dart` — метод `_toggleItem` (рядки 32-53):
   - Додати `import 'dart:convert';`
   - Замінити ДВА createPendingOp на ОДИН з `doctype: 'Checklist Instance'` та additive:
     `{'checklist_instance_item': [{'_uuid': item.itemUuid, 'checked_by': checked ? 'current_user' : null}]}`

5. `riad_mobile/lib/ui/scan/scan_screen.dart` — callback `onDetect` (рядки 49-55):
   - Додати `import 'dart:convert';`
   - Замінити `doctype: 'VisitMaterial'` на `doctype: 'Visit'`
   - Замінити payload на `jsonEncode({'scalars': {}, 'additive': {'visit_material': [{'_uuid': clientUuid, 'serial_no': code, 'item_name': code, 'qty': 1}]}})`

## Тестування
Після змін кожного файлу запускати:
```bash
cd "/home/joker/RIAD CRM/riad_mobile" && flutter analyze [шлях_до_файлу]
```
В кінці — загальний аналіз:
```bash
cd "/home/joker/RIAD CRM/riad_mobile" && flutter analyze lib/ui/visit/visit_detail_screen.dart lib/ui/media/camera_screen.dart lib/ui/voice/voice_note_screen.dart lib/ui/checklist/checklist_screen.dart lib/ui/scan/scan_screen.dart
```
Очікуваний результат: `No issues found!`

## Лог
Записати результат у файл: `.mimocode/plans/e4-session1-log.md`
```

---

## ПРОМТ СЕСІЇ 2: Background sync + Retry + Тест (Task 7-11)

```
Ти — Flutter розробник. Виконай завдання.

## Контекст
Є `sync_client.dart` з методами `pullDelta()`, `pushPending()`, `watchPendingCount()`.
Є `media_upload_service.dart` з методом `uploadPending()`.
PendingOps таблиця має поле status: pending/inflight/failed/done.
Failed PendingOps залишаються failed назавжди — retry немає.
Немає background sync — все викликається вручну.
Тест `sync_client_test.dart` рядок 589 викликає приватний метод `_handleConflict`.

## Завдання

1. Запустити існуючі тести (Task 7-8):
```bash
cd "/home/joker/RIAD CRM" && python -m unittest tests.s1.test_s1_sync -v
cd "/home/joker/RIAD CRM/riad_mobile" && flutter test test/s2/sync_client_test.dart
```

2. Створити файл `riad_mobile/lib/data/sync/background_sync_service.dart` (Task 9):
```dart
import 'dart:async';
import 'package:connectivity_plus/connectivity_plus.dart';
import 'sync_client.dart';
import 'media_upload_service.dart';

class BackgroundSyncService {
  final SyncClient _syncClient;
  final MediaUploadService _mediaUploadService;
  Timer? _timer;
  bool _syncing = false;

  BackgroundSyncService({
    required SyncClient syncClient,
    required MediaUploadService mediaUploadService,
  })  : _syncClient = syncClient,
        _mediaUploadService = mediaUploadService;

  void start({Duration interval = const Duration(minutes: 5)}) {
    _timer?.cancel();
    _timer = Timer.periodic(interval, (_) => syncNow());
    Connectivity().onConnectivityChanged.listen((results) {
      final isOnline = results.any((r) => r != ConnectivityResult.none);
      if (isOnline) syncNow();
    });
    syncNow();
  }

  void stop() {
    _timer?.cancel();
    _timer = null;
  }

  Future<void> syncNow() async {
    if (_syncing) return;
    _syncing = true;
    try {
      await _syncClient.retryFailed();
      await _syncClient.pushPending();
      await _syncClient.pullDelta();
      await _mediaUploadService.uploadPending();
    } catch (_) {
    } finally {
      _syncing = false;
    }
  }
}
```

3. Додати метод `retryFailed` в `riad_mobile/lib/data/sync/sync_client.dart` (Task 10):
```dart
Future<void> retryFailed() async {
    final failedOps = await (_db.select(_db.pendingOps)
      ..where((t) => t.status.equals('failed')))
        .get();
    for (final op in failedOps) {
      final createdAt = DateTime.fromMillisecondsSinceEpoch(op.createdAt);
      final elapsed = DateTime.now().difference(createdAt);
      if (elapsed.inMinutes < 5) continue;
      await _db.updatePendingOpStatus(op.id, 'pending');
    }
  }
```
Додати після метода `watchPendingCount()` (рядок 461).

4. Виправити тест `riad_mobile/test/s2/sync_client_test.dart` рядок 589 (Task 11):
   Замінити `syncClient._handleConflict` на `syncClient.handleConflict`

## Тестування
```bash
cd "/home/joker/RIAD CRM/riad_mobile" && flutter analyze
cd "/home/joker/RIAD CRM/riad_mobile" && flutter test test/s2/sync_client_test.dart
cd "/home/joker/RIAD CRM" && python -m unittest tests.s1.test_s1_sync -v
```

## Лог
Записати результат у файл: `.mimocode/plans/e4-session2-log.md`
```

---

## ПРОМТ СЕСІЇ 3: Drive bridge + Tombstone guard + Integration tests (Task 12-15)

```
Ти — Flutter + Python розробник. Виконай завдання.

## Контекст
Є `media_upload_service.dart` який завантажує файли на сервер. Після upload оновлює MediaAsset drive_file_id.
Але він НЕ створює PendingOp для sync — тому зміна не потрапляє на інші пристрої.
Є `database.dart` з Drift таблицями. Tombstoned записи (riadDeleted=true) можна "воскресити" створивши новий запис з тим самим UUID.
Немає інтеграційних тестів для повного sync flow.

## Завдання

1. Додати drive upload → sync bridge в `riad_mobile/lib/data/sync/media_upload_service.dart` (Task 12):
   - Додати `import 'dart:convert';`
   - Після рядка 54 (`await db.updatePendingMediaUploadStatus(upload.id, 'done')`) додати:
   ```dart
   if (driveFileId != null) {
               await db.createPendingOp(PendingOpsCompanion.insert(
                 doctype: 'Media Asset',
                 name: upload.clientUuid,
                 op: 'update',
                 payload: jsonEncode({
                   'scalars': {'drive_file_id': driveFileId},
                   'additive': {},
                 }),
                 createdAt: DateTime.now().toUtc().millisecondsSinceEpoch,
               ));
             }
   ```

2. Додати tombstone resurrection guard в `riad_mobile/lib/data/local/database.dart` (Task 13):
   Додати після метода `watchChecklistItems` (рядок 412):
   ```dart
   Future<bool> isTombstoned(String clientUuid) async {
       final visit = await (select(visits)
             ..where((t) => t.clientUuid.equals(clientUuid)))
           .getSingleOrNull();
       if (visit != null) return visit.riadDeleted;
       final instance = await (select(checklistInstances)
             ..where((t) => t.clientUuid.equals(clientUuid)))
           .getSingleOrNull();
       if (instance != null) return instance.riadDeleted;
       final map = await (select(installationMaps)
             ..where((t) => t.clientUuid.equals(clientUuid)))
           .getSingleOrNull();
       if (map != null) return map.riadDeleted;
       return false;
     }
   ```

3. Створити інтеграційний тест `tests/s1/test_s1_sync_integration.py` (Task 14):
   Повний тест push → conflict → resolve → pull cycle.
   Файл знаходиться в плані вище (Task 14, Step 1).

4. Запустити всі тести (Task 15):
```bash
cd "/home/joker/RIAD CRM" && python -m unittest tests.s1.test_s1_sync -v
cd "/home/joker/RIAD CRM" && python -m unittest tests.s1.test_s1_sync_integration -v
cd "/home/joker/RIAD CRM/riad_mobile" && flutter test test/s2/sync_client_test.dart
cd "/home/joker/RIAD CRM/riad_mobile" && flutter analyze
```

## Лог
Записати результат у файл: `.mimocode/plans/e4-session3-log.md`
```

---

## Обмеження моделі (mimo-auto)

mimo-auto — lightweight модель. Обмеження:
- **Контекст:** обмежений, не давати >3000 рядків коду в одному промті
- **Точність:** для складних змін у 5+ файлах одночасно — високий ризик помилок
- **Рекомендація:** РОЗДІЛИТИ на 3 сесії

| Сесія | Завдання | Файлів | Час |
|-------|----------|--------|-----|
| 1 | Payload format (БЛОКЕР) | 5 | 30 хв |
| 2 | Background sync + retry + тест | 4 | 1 год |
| 3 | Drive bridge + guard + tests | 3 | 1 год |
