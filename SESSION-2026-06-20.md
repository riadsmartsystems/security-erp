# Сесія 2026-06-20 — Аудит та виправлення репозиторію RIAD CRM

## Що зроблено

### 1. Створено `CLAUDE.md`

**Файл:** `CLAUDE.md` (корінь репозиторію)  
**Мета:** Документ для майбутніх інстанцій Claude Code — щоб швидко орієнтуватися в архітектурі без читання десятків файлів.

**Що включено:**
- Команди запуску стеку (`docker compose up -d`, ініціалізація ERPNext-сайту, генерація API-ключа)
- CI/CD pipeline (GitHub Actions: flake8, black, py_compile, unit tests, Docker build)
- Запуск load-тестів через `k6`
- Архітектурна діаграма потоку запиту (Browser → Traefik → security-api → Frappe → MariaDB)
- Таблиця всіх Docker-контейнерів з поясненням ролі кожного
- Внутрішня структура `services/security-api/` (модулі auth, database, routes, permissions)
- Опис кастомного Frappe-додатку `security_erp` (hooks, DocTypes по доменах FSM/CMDB/Sales/AI)
- Bench-команди для роботи з ERPNext
- Скрипти міграції даних (4 хвилі CSV → Frappe REST API)
- Змінні середовища

---

### 2. Аудит протиріч STAGE-0

**Джерело:** `STAGE-0.md` (аудит від 2026-06-18, 9 протиріч П-1…П-9)

Перевірено поточний стан кожного протиріччя — більшість вже виправлені попередніми комітами (зокрема `3a742a2 fix: cleanup docker-compose, виправлення 7 проблем`):

| # | Протиріччя | Стан до сесії | Дія в сесії |
|---|---|---|---|
| П-1 | postgres/NATS поза MariaDB | ✅ CUT-сервіси вже прибрані з compose | — |
| П-2 | Конфлікт порту 8000 (erpnext-backend vs security-api) | ✅ security-api прибраний з compose | — |
| П-3 | Хибний шлях монтажу `./security_erp_app` | ✅ Виправлено на `./erpnext/security_erp` | — |
| П-4 | `Dockerfile.backend` не задіяний у compose | ✅ `erpnext-backend` тепер будується через нього | — |
| П-5 | Залишки `services/fsm-service/` у репо | ✅ Директорія фізично відсутня | — |
| П-6 | README: застаріла таблиця сервісів | ⚠️ Перелічені видалені сервіси | **Виправлено** |
| П-7 | `security_erp` vs `security_erp_app` у назвах | ✅ Жодних зайвих посилань | — |
| П-8 | `go-live-checklist.md` описує неіснуючий стан | ⚠️ ✅ для видалених компонентів | **Виправлено** |
| П-9 | `android-app/pubspec.yaml` відсутній | ✅ Папка в `.gitignore` | — |

---

### 3. Виправлення П-6 — `README.md`

**Файл:** `README.md`

**Зміни:**

`## Сервіси` — таблиця скорочена з 8 до 3 рядків:
- Видалені: `Telegram Service`, `n8n`, `MinIO Console`, `Grafana`, `Prometheus`
- `Traefik` виправлено: порт `8080` → `80` (відповідно до `docker-compose.yml`)
- `Security API` позначено як `DEFER — не в compose`
- `ERPNext` доповнено зовнішньою URL `https://erp.riad.fun`

`## Моніторинг` — секція повністю видалена (Prometheus, Grafana, Loki — CUT).

`## Фази розробки`:
- Phase 1: прибрано `Telegram` (CUT)
- Phase 1.5: додано `Security API Gateway (DEFER)`

---

### 4. Виправлення П-8 — `docs/go-live-checklist.md`

**Файл:** `docs/go-live-checklist.md`

**Зміни:**

| Секція | Що змінено |
|---|---|
| Infrastructure | `20 контейнерів` → `10`; прибрано рядки про MinIO, NATS, PostgreSQL, Prometheus/Grafana/Loki |
| Single Database Architecture | Прибрано дублікат рядка про PostgreSQL/n8n |
| Telegram Bot | Секція повністю видалена (CUT) |
| n8n Workflows | Секція повністю видалена (CUT) |
| CI/CD | Прибрано `telegram-service` з Docker build; прибрано `Bandit security scan` (відсутній у `.github/workflows/ci.yml`) |
| Service URLs | Скорочено з 6 до 2 рядків; прибрано n8n, Grafana, Telegram Bot, MinIO; `Security API` позначено `(DEFER)` |

---

## Що НЕ зроблено (залишається)

- **Git-коміт** — зміни збережені на диску, але не закомічені.
- **`docs/PROJECT-PLAN-v3.md`** — не перевірявся на актуальність.
- **`README.md` → `## Архітектура`** — ще містить згадки `Telegram Service` і `n8n` у bullet-list (не чіпалося, бо не було в пропозиції).
- **`security_erp_app/`** — неtracked директорія у кореня; статус (чи це новий Flutter-застосунок чи артефакт) не з'ясовано.
