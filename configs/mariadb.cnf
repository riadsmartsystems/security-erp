[mysqld]
character-set-server  = utf8mb4
collation-server      = utf8mb4_unicode_ci
innodb_buffer_pool_size = 512M
innodb_log_file_size    = 128M

# Binary log (PITR) — R5-FIX-1
log_bin            = /var/lib/mysql/mariadb-bin
binlog_format      = ROW
sync_binlog        = 1
expire_logs_days   = 7
max_binlog_size    = 100M
