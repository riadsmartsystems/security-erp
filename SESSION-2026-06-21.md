# Сесія 2026-06-21 — Виконання STAGE-5 (всі блоки А, Б, В)

## Контекст

Продовження роботи з гілки `cleanup`. Попередня сесія (2026-06-20) виправила
П-6 і П-8 з STAGE-0 та залишила незакомічені зміни в README.md і go-live-checklist.md.
На початку сесії ці файли були закомічені, після чого виконано весь STAGE-5.

---

## Стан репо на початок сесії

- Гілка: `cleanup`, остання синхронізація з GitHub: `393ac4a`
- Незакомічено: `README.md`, `docs/go-live-checklist.md` (modified)
- Untracked: `CLAUDE.md`, `SESSION-2026-06-20.md`, `STAGE-*.md`, `security_erp_app/`

---

## Що зроблено

### Коміти (5 шт., у порядку):

#### `faa87f2` — docs: README і go-live-checklist
- README: прибрано Telegram/n8n/MinIO/Grafana/Prometheus з таблиці сервісів і з секцій
- go-live-checklist: зменшено к-сть контейнерів 20→10, видалено секції Telegram/n8n

#### `71d2914` — docs: CLAUDE.md і STAGE-документи
- Закомічено CLAUDE.md, STAGE-1..5.md, SESSION-2026-06-20.md, STAGE-3-PROGRESS-2026-06-20.md

#### `d99334f` — fix(infra): інфраструктурні виправлення
- `configs/mariadb.cnf` — створено (відсутність = Docker монтував директорію = MariaDB краш)
- `.env.example` — додано секцію security-api (SECRET_KEY, FRAPPE_API_KEY, FRAPPE_API_SECRET, FRAPPE_PASSWORD, FRAPPE_URL, REDIS_URL, ANTHROPIC_API_KEY)
- `scripts/deploy.sh` — додано перевірку `.env`; прибрано telegram-service/n8n; виправлено health check
- `.github/workflows/ci.yml` — прибрано telegram-service build; додано syntax check `erpnext/security_erp/`; erpnext-backend у Docker build

#### `fb35407` — fix(app): виправлення бізнес-логіки
- `events.py` — видалено `_notify_n8n()`, обидва виклики і `import httpx` (n8n більше немає)
- `permissions.py` — `frappe.has_permission()` тепер отримує `doc=doc` (перевірка конкретного документа)
- `contract.py` — `before_insert()`: замість `pass` → `self.contract_number = self.name`
- `contract.json` — додано поле `quotation` (Link → Quotation)
- `estimate.py` — додано `apply_template()` whitelisted method
- `estimate.json` — додано поле `lead` (Link → Lead)

#### `7e5da4f` — feat(app): AI estimate, Warranty Letter, Print Formats
- `estimate_utils.py` — `generate_ai_estimate(doc_name)`: читає `tz_text`, викликає Anthropic API (claude-sonnet-4-6), заповнює items у Estimate
- `doctype/warranty_letter/` — новий DocType: letter_number, installation_act, customer, issue_date, warranty_months, expiry_date (auto-calc), status
- `print_format/security_kp/security_kp.html` — Jinja2 шаблон КП для Quotation
- `print_format/installation_act/installation_act.html` — Jinja2 шаблон Акту виконаних робіт (з серійними номерами і підписами)
- `print_format/warranty_letter/warranty_letter.html` — Jinja2 шаблон Гарантійного листа

---

## Що НЕ зроблено / залишки

| # | Задача | Причина |
|---|---|---|
| C-1 | Видалити `configs/n8n/` | Власник root, `rm -rf` вимагає sudo на сервері — виконати вручну: `sudo rm -rf configs/n8n/` |
| — | `git push origin cleanup` | Не пушили — залишено на розсуд |
| — | Print Formats у Frappe fixtures | HTML-файли створені, але не зареєстровані через `hooks.py` fixtures або bench — потрібен `bench migrate` після `install-app security_erp` |
| — | `estimate.js` — кнопки для apply_template/apply_scenario/generate_ai_estimate | Методи є (whitelisted), але кнопок у UI немає — потрібен Frappe Client Script або `estimate.js` |
| — | `Warranty Letter` у workspace/menu | DocType є, але треба додати до Security ERP workspace вручну через ERPNext UI або fixtures |
| — | Ротація credentials з коміту `6713798` | Якщо ті креди (env.backup) досі валідні — потрібна ротація паролів/ключів окремо |

---

## Стан після сесії

- Гілка `cleanup` має 5 нових комітів, не запушена
- Синтаксис всіх 42 Python-файлів `erpnext/security_erp/` перевірено — OK
- `security_erp_app/` — Flutter-застосунок, НЕ видаляти (session-2026-06-20 помилково вважав порожнім)
